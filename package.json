{
  "name": "pointing-poker-app",
  "version": "1.0.0",
  "description": "App for pointing User Stories.",
  "main": "server/index.js",
  "scripts": {
    "start": "export NODE_ENV=production && node server/index.js",
    "dev": "export NODE_ENV=development && nodemon server/index.js",
    "build-react": "cd client && npm install && npm run build-react-app",
    "build-node": "npm install",
    "build": "npm run build-react && npm run build-node",
    "brs": "npm run build-react && npm start",
    "brbns": "npm run build-react && npm run build-node && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "14.20.1"
  },
  "keywords": [],
  "author": "Carlos Medina",
  "license": "ISC",
  "devDependencies": {
    "@types/uuid": "^9.0.0",
    "nodemon": "^2.0.20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "save-dev": "0.0.1-security",
    "socket.io": "^4.5.4",
    "uuid": "^9.0.0"
  }
}
