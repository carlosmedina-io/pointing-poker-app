{
  "name": "pointing-poker-app",
  "version": "1.0.0",
  "description": "App for pointing user stories.",
  "main": "index.js",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd react-app && npm install && npm run build-react-app",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "14.20.1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
